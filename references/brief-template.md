# 开工简报模板

Anthropic 那次冲刺开始前，给频道里的 Claude 留了一段常驻指令，原文大意：

> 你的工作是负责 claude.ai 网站和桌面 App 性能相关的一切：监控每次部署有没有带来性能退化；评估现有遥测数据是否准确、全面；维护整理得当的可观测性看板；针对发现的问题和唾手可得的改进，主动去实现；提出值得做的性能项目；和人类同事沟通。这个频道的终极目标，是让你尽可能自主，但我们知道今天还做不到。

下面是把它改写成单个项目用的版本，复制到 `perf/BRIEF.md` 后填空。

---

# 性能冲刺简报：<项目名>

## 你的职责

负责 <项目名> 打开和使用的速度：先测基线，再一步步优化，每一步都用测量证明，赢下来的用棘轮锁住。主动提出改进，但用户看得见的变化和上线由人决定。

## 核心操作与「能用」的定义

| 操作 | 页面 | 起点 | 终点（真的能用） | 判定表达式 |
|---|---|---|---|---|
| 例：打开编辑器 | / | 导航开始 | 编辑框能输入，输入的字不会丢 | `document.querySelector('textarea')?.offsetParent !== null` |

## 不能动的东西

- 视觉：
- 功能：
- SEO / 统计：

## 目标

- 主指标：<操作> 的 p75 从 <基线> 降到 <目标>（降幅 <x>%）
- 测量口径：见 `references/measurement-protocol.md`，写明用的是哪个档

## 上线与回滚

- 谁批准上线：
- 部署方式：
- 回滚办法：
